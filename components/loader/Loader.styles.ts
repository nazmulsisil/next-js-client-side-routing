import styled from 'styled-components';

export const InlineWrapper = styled.span`
  font-size: 0.8em;
`;

export const FullPageWrapper = styled.span`
  font-size: 8rem;
  display: flex;
  align-items: center;
  justify-content: center;
  height: 80vh;
`;

export const AbsoluteWrapper = styled.span`
  font-size: 4rem;
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  display: flex;
  align-items: center;
  justify-content: center;
`;
